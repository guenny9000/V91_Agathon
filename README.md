# V91-Agathon

This is the V91-Agathon repository that implements the V91 functionalities. This README provides an overview of the project and how to set it up.

## Installation

To install the required packages, run:

```bash
pip install -r requirements.txt
```

## Usage

To run the example pipeline, execute:

```bash
python examples/simple_pipeline.py
```
